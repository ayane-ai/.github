# 贡献指南

适用于 `ayane-ai` 组织下的全部仓库。当前项目由个人维护，以下约定用于保持提交历史和文档可读。

## 提交信息

采用 Conventional Commits，scope 使用仓库名或模块名：

```text
docs(architecture): 补充客户端与 Unity 的边界说明
feat(client): 增加会话流式响应
fix(agent-service): 修正记忆召回的时间窗口
```

## 分支

- 文档仓库 `ayane-docs` 允许直接在 `main` 上提交与推送。
- 代码仓库使用 `agent/<topic>_<yyyymmdd>`，只含小写字母、数字和下划线，例如 `agent/user_auth_20260401`。

## 合并请求

- 说明变更动机、影响范围和验证方式。
- 小步提交，不启用自动合并；合并前必须有人看过 diff。
- 验证结论必须来自实际执行的检查。

## 文档改动

- 最小修改：只处理指定范围，不顺手重构章节或重新设计架构。
- 保留已有正文、图表、代码块和示例，不以较新的导出覆盖已确认的决策。
